<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Data retention periods (years)
    |--------------------------------------------------------------------------
    |
    | Периоди на съхранение за автоматично почистване (prune команди).
    | Записи по-стари от прага се изтриват физически при schedule:run.
    |
    */

    'audit_logs_years' => (int) env('AUDIT_LOG_RETENTION_YEARS', 1),

];
